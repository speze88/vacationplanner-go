services:
  urlaubsplaner:
    # build: .
    image: ghcr.io/speze88/vacationplanner-go:1.0.0
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - URLAUBSPLANER_ADMIN_USER=admin
      - URLAUBSPLANER_ADMIN_PASS=changeme
    restart: unless-stopped
